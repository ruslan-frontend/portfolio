import Head from "next/head";

const Contacts = () => {
    return (
        <div>
            <Head>
			<title>Portfolio | Contacts</title>
		</Head>
            <h1>Contacts</h1>
        </div>
    )
}

export default Contacts;